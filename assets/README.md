# assets

Profil fotoğrafını buraya `profile.jpg` olarak koy.
Put your profile photo here as `profile.jpg`.

Farklı bir isim/uzantı kullanırsan `js/data.js` içindeki `photo` yolunu güncelle.
If you use a different name/extension, update the `photo` path in `js/data.js`.
